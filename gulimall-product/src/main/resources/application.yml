spring:
  application:
    name: gulimall-product
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: product
        group: dev
  config:
    import:
      - optional:nacos:gulimall-product.yml?refreshEnabled=true&group=dev
      - optional:nacos:datasource.yml?refreshEnabled=true&group=dev
      - optional:nacos:mybatis.yml?refreshEnabled=true&group=dev